import 'package:flutter/material.dart';

class StudentStartExamForm extends StatelessWidget {
  const StudentStartExamForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Start Exam")),
      body: const Center(child: Text("Exam Form Skeleton")),
    );
  }
}
